PORT=3000
APP_URL=http://localhost:3000
DATA_DIR=./data

# Supabase (required for production persistence; local dev falls back to file-based storage)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: enables LLM-powered idea generation (falls back to templates without it)
OPENAI_API_KEY=
